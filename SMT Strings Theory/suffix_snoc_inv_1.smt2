(set-logic UFSLIA)
(declare-fun s1 () String)
(declare-fun s2 () String)
(declare-fun sx () String)
(declare-fun sy () String)
(assert (= (str.len sx) 1))
(assert (= (str.len sy) 1))
(assert  (str.suffixof (str.++ s1 sx) (str.++ s2 sy) ))
(assert (not (= sx sy )))
(check-sat)
(exit)
